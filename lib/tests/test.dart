import 'package:test/test.dart';
import 'package:bloc_test/bloc_test.dart';

void main() {
  group('main', () {
    setUp(() {});
    tearDown(() {});
  });

  group('models', () {
    setUp(() {});
    tearDown(() {});
  });

  group('data', () {
    setUp(() {});
    tearDown(() {});
  });
}
